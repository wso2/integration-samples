configurable record {
    string spaceId;
    string token;
    string key;
} googleChatConfig = ?;

configurable record {
    string refreshToken;
    string clientId;
    string clientSecret;
    string refreshUrl;
    string baseUrl;
} salesforceConfig = ?;
