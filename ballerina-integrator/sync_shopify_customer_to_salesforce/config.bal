configurable string shopifyApiSecretKey = ?;

configurable string salesforceBaseUrl = ?;
configurable string salesforceClientId = ?;
configurable string salesforceClientSecret = ?;
configurable string salesforceRefreshToken = ?;
configurable string salesforceRefreshUrl = ?;

configurable string? defaultRecordType = ();
configurable string? defaultLeadSource = ();
configurable string? defaultOwnerId = ();

configurable string accountAssociationRule = "MATCH_BY_DOMAIN";